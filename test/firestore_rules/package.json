{
  "name": "shoe-store-rules-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Firestore security-rules tests for the multi-tenant Shoe Store",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project shoe-store-rules-test \"mocha rules.test.js --timeout 20000\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.12.0",
    "firebase-tools": "^13.11.0",
    "mocha": "^10.4.0"
  }
}
